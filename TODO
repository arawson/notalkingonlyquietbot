Implement some kind of cheap natural language processing instead of ! commands
Implement removal of dead links
Determine if dead links can be removed automatically
Fart with reverb (admins adding categories of memes, that then get pushed in manually, and later, automatically)